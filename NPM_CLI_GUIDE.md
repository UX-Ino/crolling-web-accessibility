# 📦 npm CLI 도구로 배포하기

## 🎯 개요

Electron 앱 대신 **npm 패키지**로 배포하면:
- ✅ 크로스 플랫폼 호환성 문제 없음
- ✅ 간단한 설치 (`npm install -g`)
- ✅ 자동으로 크롬 브라우저 실행
- ✅ Electron 없이 Next.js만으로 동작

## 🚀 사용 방법

### 설치
```bash
npm install -g a11y-crawler
```

### 실행
```bash
a11y-crawler
```

**자동으로:**
1. 로컬 서버 시작 (http://localhost:3000)
2. 크롬 브라우저 자동 실행
3. 웹 UI에서 크롤링 작업

### 종료
```bash
Ctrl + C
```

---

## 📝 배포 준비

### 1. 빌드
```bash
# Next.js 프로덕션 빌드
npm run build

# CLI 스크립트 실행 권한 부여
chmod +x bin/cli.js
```

### 2. 로컬 테스트
```bash
# 로컬에서 전역 설치 테스트
npm link

# 실행
a11y-crawler

# 테스트 완료 후 제거
npm unlink -g a11y-crawler
```

### 3. npm 배포
```bash
# npm 로그인 (처음만)
npm login

# 버전 업데이트
npm version patch

# 배포
npm publish

# 특정 태그로 배포 (베타 버전 등)
npm publish --tag beta
```

---

## 🔧 개발자용 스크립트

현재 `package.json`에 추가된 설정:

```json
{
  "name": "a11y-crawler",
  "version": "1.0.0",
  "bin": {
    "a11y-crawler": "./bin/cli.js"
  },
  "files": [
    "bin/",
    "app/",
    "lib/",
    "out/",
    "package.json"
  ]
}
```

---

## 🌍 전 세계 배포 후

누구나 이렇게 사용 가능:

```bash
# 설치
npm install -g a11y-crawler

# 실행
a11y-crawler

# 업데이트
npm update -g a11y-crawler

# 제거
npm uninstall -g a11y-crawler
```

---

## 💡 장점

| 항목 | Electron 앱 | npm CLI 도구 |
|------|-------------|--------------|
| 설치 | DMG 다운로드 + 설치 | `npm i -g` 한 줄 |
| 크기 | ~200MB | ~50MB |
| 호환성 | macOS 버전 제한 | Node.js만 있으면 OK |
| 업데이트 | 수동 재설치 | `npm update -g` |
| 배포 | 서명/공증 필요 | npm publish만 |

---

## 🔥 다음 단계

1. **의존성 설치**
   ```bash
   npm install open
   ```

2. **빌드**
   ```bash
   npm run build
   ```

3. **로컬 테스트**
   ```bash
   npm link
   a11y-crawler
   ```

4. **배포**
   ```bash
   npm publish
   ```

---

## ⚠️ 주의사항

### 배포 전 체크리스트
- [ ] `package.json`의 `name`이 npm에서 사용 가능한지 확인
  ```bash
  npm view a11y-crawler
  # "npm ERR! 404" 나오면 사용 가능
  ```
- [ ] `version` 업데이트
- [ ] `README.md` 작성
- [ ] `LICENSE` 파일 추가
- [ ] `.npmignore` 또는 `files` 설정 확인

### 이름이 이미 사용 중이라면?
```json
{
  "name": "@your-username/a11y-crawler"
}
```

---

## 📖 README 예시

배포 시 프로젝트 루트에 이 내용을 `README.md`로 추가:

\`\`\`markdown
# 웹 접근성 크롤러

KWCAG 2.2 기준 웹 접근성 자동 검사 도구

## 설치
\`\`\`bash
npm install -g a11y-crawler
\`\`\`

## 사용법
\`\`\`bash
a11y-crawler
\`\`\`

브라우저가 자동으로 열리며, URL을 입력하고 크롤링을 시작하세요.

## 기능
- 🔍 전체 사이트 크롤링
- ♿ axe-core 기반 접근성 검사
- 📊 Excel/HTML 리포트 생성
- 🎯 KWCAG 2.2 매핑
\`\`\`

---

이제 바로 시작할까요? 필요한 패키지 설치부터 시작하겠습니다!
